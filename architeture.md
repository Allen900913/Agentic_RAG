# RAG 架構總覽

本文件說明 `rag_query.py` 檢索/生成 pipeline 與 `eval/` 評測系統的架構決策——每個階段「用什麼」以及「為什麼」。細節佐證與實驗數據見 `CHANGELOG.md`；本文件只整理成型的結論與其背後的推理，不重複記錄失敗實驗的過程（那是 CHANGELOG 的工作）。

---

## 總覽表

| 階段 | 一句話 | 用什麼模型 / 參數 |
|---|---|---|
| 召回:中文搜英文 | BGE-M3 多語言,中文問句和英文語料同一向量空間 | 單一 BGE-M3(dense+sparse) |
| Rewrite 對齊用詞 | 本質是盲猜語料具體用詞,靠詞彙表而非大模型,又便宜又穩 | 小模型 70b + glossary |
| CAP-union 保候選 | 不用全域 RRF(會稀釋單變體 chunk),改原句全留+每變體 top-8 聯集;CAP 是控噪上限,調到 8 平衡誤殺與進垃圾 | RRF_TOP_N=20 / VARIANT_CAP=8 |
| Rerank | cross-encoder 精排;發現中問英答會失真,翻譯修法(`translate_en`)驗證範圍已擴大(k=3+lexical/mixed零回歸)但仍未轉正,待端到端全量驗證 | bge-reranker-v2-m3 |
| 生成 | 難點是「把 context 關鍵事實穩定寫進答案」,需模型能力,小模型會漏 | 大模型 120b |
| Rubric | 權重必須對齊問題重心、不能要求語料沒有的東西;寫法直接決定 judge 判得對不對 | 語意編碼進 schema |
| Judge | 拆成二元判斷讓小模型原則上夠用,但實測有系統性 bug,續用 20b 是成本妥協非技術最優 | 20b(有已知漏洞,120b 可修但吃額度) |

Pipeline 順序：`query → [召回: dense+sparse] → [rewrite 擴召回] → [CAP-union 合併] → [rerank 精排] → [生成]`，`eval/` 另外用 `rubric + judge` 對這條 pipeline 的輸出評分，是獨立於生產 pipeline 之外的第二套系統，但兩者互相影響（judge 判得準不準，直接決定「retrieval/生成有沒有真的變好」這個問題能不能被回答）。

---

## 1. 召回：中文搜英文（BGE-M3）

**用什麼**：`BAAI/bge-m3`（`FlagEmbedding.BGEM3FlagModel`），一次 `encode()` 同時輸出 dense 向量（1024-D，cosine）與 sparse lexical-weight dict，兩路都送進 Qdrant 做 server-side RRF 融合。

**為什麼中文問句能搜到英文財報**：BGE-M3 是原生多語言模型，中文與英文文本會被投影到同一個語意向量空間，語意相近的中英文 pair 之間的 cosine 相似度本來就高。這不是系統刻意做了什麼「翻譯層」，而是**選對了一個本身具備跨語言對齊能力的 embedding 模型**，所以中文查詢對英文語料的 dense 檢索天生可用。

**這個事實是後面好幾個決策的前提**：
- 正因為 dense/sparse 召回階段中文本來就 work，後續有人嘗試「把召回階段的問句也翻成英文」，反而引入了不必要的用詞漂移，被證實有害（見 CHANGELOG 2026-07-08「重構 translate_query_en」）——sem-11 從穩定命中退步成排序邊緣，原因是翻譯後的英文措辭改變了 RRF 候選池的組成，把本來排得上的 chunk 擠掉。
- 這也是為什麼**只有 rerank 階段才翻譯、召回階段維持原文**這個架構決策成立的原因（見第 4 節）。

**已知限制**：多語言對齊不是萬能的——語意相近但用詞完全不同的情況（例如中文問「策略方向」，語料寫的是具體會計術語如「營業利益」）依然會因為兩者在向量空間裡距離較遠而檢索不到，這個落差由下一階段的 rewrite 來補。

---

## 2. Query Rewrite：對齊語料用詞

**用什麼**：小模型（`llama-3.3-70b-versatile`，`DEFAULT_MODEL`）+ 手工維護的 `_COMPANY_PRODUCT_GLOSSARY`（per-company 產品/分部詞彙表）。

**核心洞察：rewrite 的本質是「盲猜語料裡的具體用詞」，不是「換句話說」**。問「微軟的 AI 戰略」，語料裡實際寫的可能是「Copilot」「Azure AI」「Productivity segment」這些具體詞彙。這是一個封閉世界的猜詞任務——語料就是固定的 7 家公司財報/新聞，具體用詞是有限、已知、可窮舉的，不需要靠模型的世界知識去賭。

**為什麼小模型就夠**：如果把 rewrite 當成「靠模型自己的知識庫猜對詞」，那確實需要更強的模型（120b 的 rewrite 變體能猜到 Copilot，70b 猜不到，這是實測差異）。但**换一個更便宜的解法：直接把已知詞彙表餵給小模型**，讓它不用猜，這樣 70b 也能穩定生成含正確詞彙的變體。詞彙表建立原則：**每一項都先用 grep 在實際語料（`data/unstructure_processed/*.txt`）裡確認詞彙真的出現過才收錄**，不臆測——例如曾經想收錄 AWS 的 Bedrock/Trainium/SageMaker，grep 後發現語料裡從未出現，全部剔除。

**踩過的坑**：第一版只是把詞彙表丟給模型，沒有要求「變體要分散覆蓋不同詞彙」，結果模型仍然收斂到清單裡最顯眼的那個詞（例如永遠只寫 Azure AI，不寫 Copilot）——**有清單可查，不代表會主動探索**，這跟 rewrite 本身「不主動猜、只給保守答案」的傾向是同一種模式。加上明確的「分散覆蓋」指令後才穩定改善。

**這一步解決的只是三層失敗（recall → rank → generation）裡的第一層**：詞彙表能讓正確 chunk 從「完全不在候選池」進步到「進池」，但進池之後能不能被 rerank 排進最終 top-k，是下一階段的問題，兩者是接力關係。

---

## 3. CAP-union：候選池合併架構

**用什麼**：`RRF_TOP_N_PRIMARY = 20`（原始 query 一路的候選數，不砍）、`VARIANT_CAP = 8`（每個 rewrite 變體最多貢獻幾個候選）。

**保護正確 chunk 的關鍵在架構選擇，不是 CAP 這個數字本身**。曾經測試過的兩種合併方式：

1. **全域 RRF 融合**（原句 + 所有變體丟進同一個 RRF 排名，跨查詢算「名次倒數和」）——**已證實會反效果**。如果一個 chunk 只被某個變體命中、其他變體都漏掉它，融合後這個 chunk 的分數會被其他變體的「沒看到它」拉低、被稀釋掉。這正是「RRF 會把我要的 chunk 排除掉」這個現象的真正機制。
2. **CAP-union（目前生產架構）**——原始 query 最可信，全部保留（20 個，不砍）；每個變體**各自獨立**取自己最有信心的 top-`VARIANT_CAP` 個候選，然後跟原句的候選做**聯集去重**。這樣設計下，一個 chunk 只要能被**任何一個變體**排進它自己的 top-8，就能穩定進入最終候選池，**不會被其他「沒看到它」的變體拖累**——因為聯集架構下，變體之間沒有互相拉低分數的機制，各自獨立貢獻。

**CAP=8 這個數字的角色是「控噪閘門」，不是保護機制本身**。變體的可信度不如原始 query（它是模型生成的猜測性查詢），如果不設上限，一個措辭很差的變體可能塞進一堆不相關的候選稀釋整個池子。CAP 值的調整記錄：從舊值 5 調到現行的 8，是把這個閘門開大一點，讓變體裡「排第 6~8 名但確實有價值」的候選也有機會進池，同時仍然限制爛變體的破壞範圍。

**實際池大小**：`pool = primary_20 ∪ (每個變體 top-8，去重)`，因為變體之間、變體與原句之間常有重疊，實際池大小落在 20~33 之間，不是簡單的 20+8×N。

---

## 4. Rerank：cross-encoder 精排

**用什麼**：`BAAI/bge-reranker-v2-m3`，對 CAP-union 產生的候選池（`RERANK_INPUT_N=50` 上限內）逐一評分，取 `DEFAULT_TOP_K=5` 個送進生成。

**重大發現：cross-encoder 對「中文問句 ↔ 英文文件」這種跨語言配對的相關性評分會系統性失真**。決定性實驗：同一段講 EV 價格戰的文本（`TSLA_10K_2025.html` chunk #67），同一個候選池，只換評分用的問句語言——

| 評分用問句語言 | cross-encoder 原始分數 | 池內排名 |
|---|---|---|
| 繁體中文（原始問句） | 0.3893 | 第 5（壓線）|
| 英文（翻譯後） | 0.9427 | 第 1 |

且原本靠中文問句排在前面、但主題其實不相關的候選（人才競爭、審計相關段落），翻成英文評分後名次全部下滑——顯示翻譯不只拉高了正確候選，也同時降低了假陽性，不是單純運氣。

**為什麼只有 rerank 翻譯、召回不翻譯**：召回（dense/sparse）是在整個語料庫裡搜尋，問句用詞的細微差異會改變「哪些候選被選中」這件事本身，而 BGE-M3 對中文原本就有不錯的跨語言對齊（見第 1 節），翻譯在這一步是淨損耗。Rerank 則是對**已經選定的固定候選集合**重新打分，不涉及「找到新東西」，這裡要修的是模型在跨語言配對上的系統性判斷偏誤，而不是搜尋範圍的問題——兩個階段面對翻譯的風險/收益結構完全不同。

**現況：這個修法（`translate_query_en` 參數）仍未轉為生產預設，目前預設關閉，但驗證範圍已大幅擴大**（見 CHANGELOG 2026-07-11）：
- sem-11 的 critical checkpoint（EV 價格戰）：k=3 穩定解決（3/3 皆進 top-5），且必須跟第 2 節的「語域轉換」規則搭配才成立——rewrite 變體先把候選撈進池（召回層），`translate_en` 再修正 rerank 對這批新候選的跨語言評分失真（排序層）；單獨開語域規則反而會退步（變體撈進的雜訊在中文 rerank 下被誤判成比正解更相關，把正解擠出 top-5），兩個修法分工、缺一不可。
- sem-08 靠語域規則本身（不需要 `translate_en`）就解決，因為它的根因是候選根本沒進池（純召回層問題），`translate_en` 只管排序，救不到它。
- lexical（15 題）+ mixed（25 題）全量回歸：correct 1.000 / 0.909，對照舊基準 1.000 / 0.897，零回歸。
- 尚未執行：semantic（11 題）+ colloquial（10 題）全量 k=3——這是決定是否把「語域規則 + `translate_en`」轉為生產預設的最後一步。

**結論**：跨語言失真是真實存在且已證實的問題，`translate_en` 是有效的部分解法，但**必須與語域轉換規則搭配才能同時補上召回與排序兩層**，單獨使用任一個都不完整；轉不轉生產預設待全量驗證後決定。

---

## 5. 生成（Generation）

**用什麼**：`openai/gpt-oss-120b`（`DEFAULT_GEN_MODEL`），透過 Groq；溫度 `GEN_TEMPERATURE = 0.3`（不可用 0，見下方溫度說明）。

**核心洞察：生成階段的難點不是「模型會不會寫」，是「模型會不會把 context 裡已經存在的關鍵事實穩定寫進答案」**。這是一個容易被誤解的地方——如果正確的 chunk 已經在 top-5、已經送進了 context，直覺上剩下的只是「把它轉述出來」這種輕鬆的文字工作，理應小模型就能勝任。但實測發現並非如此：MSFT AI 戰略那題，即使含 Copilot 描述的 chunk 已經確實在 context 裡，70b 生成的答案**時有時無**（像擲骰子），而 120b **穩定**每次都把它寫進答案。

這代表小模型的弱點不是文字生成能力，而是**在多個候選事實之間做取捨與覆蓋的穩定性**——面對一段包含多個可能得分點的 context，小模型容易挑其中一兩個顯眼的來寫、隨機漏掉其他同樣重要的點；大模型在這件事上更可靠。11 題乾淨對照實驗（固定住檢索側，只換生成模型）顯示 120b 對 70b **零回歸、多題從有 critical_miss 變成穩定滿分**，因此生成端轉正使用 120b。

**溫度為何是 0.3、不是 0**：曾經嘗試把生成溫度也設為 0（跟檢索側一致，追求完全確定性），結果分數不升反降。根因是 temp=0 的 greedy decoding 容易出現重複或「模式崩潰」（同一句話在開頭結尾覆述），這種重複會擠占本來可以用來覆蓋其他 rubric 得分點的內容空間，對「需要涵蓋多個要點」的評分方式不利。因此採用**溫度拆分**：檢索側（filter/rewrite）與所有 judge 呼叫維持 temp=0（要確定性、可比較），只有生成答案這一步用 temp=0.3。

**與檢索側模型的關係**：生成模型（`gen_model`，120b）與檢索側模型（`retrieval_model`，70b，用於 `parse_query_filters`/`rewrite_query`/`translate_query_to_english`）是**兩個獨立參數**，不共用同一個變數——早期曾經發生過診斷腳本把兩者混用、導致診斷結論不準確的 bug，修正後兩者徹底解耦。

---

## 6. Eval Rubric 設計

**核心原則：rubric 的權重必須對齊問題實際的重心，不能要求語料裡不存在的內容，而且 rubric 的寫法直接決定 judge 判得準不準。**

**權重要對齊問題重心，否則會產生「答對了還被扣分」的情況**：一個問「（某業務）到底在賺還在賠」的問題，如果 rubric 把「賺賠」這個問題直接問的東西設成次要得分點、把另一個附帶提及的話題設成 critical，模型認真回答了「賺還是賠」（包含具體虧損數字）反而因為 rubric 權重顛倒被判低分——這不是生成錯誤，是評分標準本身抓錯了問題的重點。

**不能要求語料裡不存在的具名定位或世界知識**：曾經出現 rubric 要求答案「把某雲端業務定位為市場三強之一、明確點名競爭對手」，但 SEC 財報原文本身從不會自我標榜市佔龍頭、也不會點名具體對手（這是 SEC filing 的一般寫作慣例，不是這幾份文件特有）。這種 rubric 等於**要求模型引入 grounding 文件之外的世界知識才能拿滿分**，而這直接跟系統自己「不准捏造未在來源出現的內容」的反幻覺原則自相矛盾。正確的修法是把 rubric 改成「有提到面臨競爭」這種資料裡真實存在、可驗證的陳述，不要求指名道姓。

**rubric 的寫法（不只是內容，包括语法符號）直接決定 judge 判得對不對**：
- checkpoint 文字裡用「/」表示「兩個條件擇一即可」（OR 邏輯），如果 judge 沒有被明確告知這個約定，會死板地只認字面上出現的第一個詞，把明明用不同措辭命中同一個概念的答案判成未命中。
- 涉及具體數字的 checkpoint（例如「毛利率約 68.3%」），如果沒有明確標注容忍區間，judge 會在「答案給 68%、rubric 要 68.3%」這種本質正確的情況上錙銖必較，或者反過來把「有具體數字」本身當成捏造嫌疑來抓，判定尺度完全不穩定。

**因此 rubric 的語意需要盡量編碼進結構化欄位（schema），而不是指望 judge 讀懂自然語言裡的隱含約定**——例如用 `tolerance_pct` 欄位明確標注數值容忍度，而不是靠 judge 自己去猜測「約」這個字代表多少誤差範圍是可以接受的。schema 化的好處是：判定邏輯是資料驅動的，未來出現新的類似案例只需要在資料裡加欄位，不需要每次都在 judge 的系統提示詞裡再堆一條新規則（規則堆得越多，彼此干擾、產生非預期副作用的風險越高）。

---

## 7. Judge

**用什麼**：`openai/gpt-oss-20b`（`DEFAULT_JUDGE_MODEL`），配合 `--judge-votes` 多數決機制。

**設計理想：把判定工作拆成一個個二元判斷（是/否命中某個 checkpoint），讓小模型原則上就能勝任**。整套 correctness 評分刻意設計成「LLM 只回傳 hit/miss 的 list，最終加權分數由 Python 依 rubric 權重計算」，不是叫 LLM 直接吐一個 0~1 的分數——這是為了避免把「複雜的整體評分判斷」這種需要更強推理能力的任務丟給模型，只留下對它來說相對單純的逐項是非判斷。基於這個拆解，理論上小模型應該足夠勝任。

**但實測發現這個理想只成立一半，是需要持續關注的已知弱點**：小模型在某些二元判斷上有**系統性**錯誤，不是隨機噪音——例如財年措辭的誤判（把公司自己財報慣用的財年命名方式誤判為「引用錯季度」）、OR 邏輯的不穩定判讀。系統性錯誤的關鍵特徵是：**3 票多數決救不回來**，因為多數決的原理是稀釋隨機噪音（每次抽樣误判的方向不同，多數會抵消少數），但系統性偏誤是模型每次都用同一套（錯誤的）判斷邏輯，votes 開到 3 甚至更多，錯誤結果不會改變。

**已驗證：換成更強的 judge 模型（`openai/gpt-oss-120b`）能穩定修好這些系統性錯誤**，但目前生產與主要 eval 腳本**沒有切換**，原因是純粹的成本/額度考量——生成階段已經在用 120b，如果 judge 也用 120b，兩者會共用同一個 Groq API 額度池（TPD），容量風險尚未評估，切換與否是需要衡量「判定準確度提升」對「額度消耗增加」的取捨決策，不是技術上做不到。

**結論**：小模型 judge 目前是一個**已知帶有特定漏洞、但基於成本考量暫時接受**的妥協狀態，不是「验证过小模型技术上完全够用」的結論。每次修改 judge 的判定邏輯或考慮更換 judge 模型，都應該用固定案例的回歸測試（含正案例與刻意構造的反案例）驗證，而不是只靠重判單一已存答案就下結論——單一案例的判定結果改變，不能證明規則在其他情境下依然可靠，也不能排除規則被改得過鬆、放過真正違規案例的風險。

---

## 附：各階段模型/參數選擇的共同邏輯

縱觀以上七個階段，一個共同的設計哲學浮現：**任務的難度不是均勻分佈的，模型/資源的投入應該對應到任務的真實難度，而不是一律用最強或最省的方案**。

- 「猜詞彙」「二元是非判斷」這種封閉世界、結構化程度高的任務，用小模型 + 結構化輔助（詞彙表、schema）就能達到大模型的效果，且更便宜、更穩定。
- 「多要點穩定覆蓋」「複雜語意判斷的系統性弱點」這種開放式、需要綜合推理能力的任務，換更強的模型有實質、可驗證的效果。
- 每一個「用小模型/簡化方案」的決策，背後都有一個「如果不這樣做，換大模型會更好嗎」的實測對照——沒有實測支持的簡化都應該被懷疑，而不是想當然爾地接受。
