# Python >= 3.10 required (developed with 3.11.9)

# === Core RAG Pipeline ===
sentence-transformers>=2.6.0
qdrant-client>=1.12.0
openai>=1.14.0
python-dotenv>=1.0.0

# === Hybrid Retrieval (Dense + Sparse via BGE-M3, server-side RRF via Qdrant) ===
FlagEmbedding>=1.2.10
langchain-huggingface>=0.0.3
langchain-experimental>=0.0.50
google-genai>=0.3.0

# === Web Frontend (FastAPI backend + Streamlit UI, SSE streaming) ===
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sse-starlette>=2.0.0
streamlit>=1.33.0

# === Data Processing Support ===
beautifulsoup4>=4.12.0
pdfplumber>=0.10.0
PyPDF2>=3.0.0

# === US Stock Data Collection ===
yfinance>=0.2.38
pandas>=2.0.0

# === News Fetching ===
curl_cffi>=0.7.0
requests>=2.31.0

# === SEC EDGAR (Financial Reports) ===
edgartools>=2.0.0

# === Eval: BM25 comparison lane (eval_retrieval.py) ===
rank-bm25>=0.2.2
jieba>=0.42.1

# === Agentic RAG (agentic_rag_version/：LangGraph 四節點) ===
# 注意：langchain-openai 會把 openai 升到 2.4x；rag_query.py 的 openai 用法在 2.x 相容。
# deepagents 是第一代 agentic（已被 LangGraph 版取代、現行程式不再 import），保留以便重現舊實驗。
deepagents>=0.6.12
langgraph>=1.2.9
langchain-openai>=1.3.5
# agentic_rag_version 的 web_search 工具（live 路徑）；需 .env 的 TAVILY_API_KEY
tavily-python>=0.5.0
