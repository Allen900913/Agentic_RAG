# eval/ 版控策略：腳本(*.py)、eval set、rubric、judge 回歸案例(judge_regression.py 內嵌)、
# 文件(*.md) 進版控；大體積且會頻繁變動的「跑分輸出 JSON」與 log 不進版控。
#
# 決策依據：所有 A/B / 迴歸決策都靠 eval_set.json + 腳本 + judge 案例，這些是專案最有價值的資產，
# 必須與 code 同版控（CLAUDE.md「eval/ 未被 git 追蹤」待辦）。跑分結果可由腳本重現，故忽略。

# 所有跑分結果 JSON 一律忽略……
*.json
# ……但標準答案 / 測試輸入集必須追蹤
!eval_set.json
!eval_set_business.json
!eval_set_agentic.json
# reference_answers.json = RAGAS 的 ground truth，跟 eval_set 一樣是「標準答案」而非跑分輸出。
# 先前被 *.json 一併忽略是漏網：它不是腳本能免費重現的東西（要花一次 LLM 全量生成，
# 且 2026-08-07 稽核後含 14 題 24 處人工校正，重生成會覆蓋掉），一旦誤刪就永久遺失
# ——本專案已經發生過未追蹤的 eval 檔被刪、git 完全救不回的情況。
!reference_answers.json

# ── 2026-08-20 補：白名單漏了六個「輸入／標準答案」類檔案 ─────────────────────────
# 上面那段自己寫著「本專案已經發生過未追蹤的 eval 檔被刪、git 完全救不回的情況」，
# 而同樣的事又發生了一次：`*.json` 是黑名單制，**新增的標準答案檔預設就被吃掉**，
# 而且不會有任何警告。判準沿用上面那一條——「腳本能不能免費重現它」：
#   · eval_set_news.json          冷凍的 37 題（拆自 eval_set.json，人工分類過）
#   · reference_answers_news.json 對應的 RAGAS ground truth（含人工校正）
#   · number_claims_news.json     對應的逐條斷言
#   · web_claims.json / web_claims_news37.json  live web 路徑的斷言（全部人工寫的）
#   · period_probe_baseline.json  期別探針的**凍結快照**——照當下 manifest 展開會讓量尺失去判別力
#   · period_probe_trend_queries.json  跨期 collapse 的陰性對照（eval_set 裡沒有這個形狀的題）
!eval_set_news.json
!reference_answers_news.json
!number_claims_news.json
!web_claims.json
!web_claims_news37.json
!period_probe_baseline.json
!period_probe_trend_queries.json

# ── 2026-08-27 補：**同一件事第三次發生**（上面那段就是第二次寫的）─────────────────
# 黑名單制的 `*.json` 又吃掉兩個「腳本不能免費重現」的檔，而且一樣沒有任何警告：
#   · period_probe_benefit_queries.json  多年語料**收益**探針的題庫。20 題歷史題的 literal
#     是人工從舊年度 filing 讀出來、逐條通過前提檢查（在單年 KB 必須查無、在多年 KB 必須查有）
#     才留下的；4 題當期陰性對照少一題，「一律把舊年份排前面」就會滿分。重建＝重做一次造題。
#   · web_replay_llm.json  `web_fixture.json`（5 題 live web）的 llm replay cache。它的
#     **兄弟檔 `web_replay_llm_news37.json` 上面已經白名單了**，這一個漏掉——理由與那個一字不差：
#     沒有它，重放會生出不同的 web query、打不到 fixture（MoE 不固定路由）。
# ⚠ 下次再新增 eval 輸入檔，**先問「它在不在白名單裡」**：這條規則是黑名單制，沉默地吃掉新檔。
!period_probe_benefit_queries.json
!web_replay_llm.json

# ── 2026-08-29 補：**第四次**。上一段的警告就寫在正上方，然後同一件事又發生了 ───────
#   · comparison_claims.json  multi_hop「哪一家最高」的逐條斷言。每一條的 compare_field
#     是**人工逐字比對題目與 reference 才定下來的**（mh-02 問的是毛利率不是淨利率，填錯這一格
#     真值會從 META 變成 NVDA ＝ 製造假缺陷），腳本重現不了。
#   · number_claims.json      早於 `*.json` 規則就被追蹤，所以沒被吃掉——但它從來不在白名單裡，
#     一旦被 `git rm --cached` 過就再也加不回來。補上，讓白名單與實際追蹤的檔案一致。
# ⚠ 這條規則已經沉默吃掉新檔四次。**新增 eval 輸入檔的同一次改動就要加白名單**，
#   不要等到 `git add` 報錯才想起來（它報錯是幸運，忘了 add 才是真正的失效模式）。
!comparison_claims.json
!number_claims.json

# ── web fixture：**體積大但仍然入版控**，這是刻意的例外 ────────────────────────────
# 它不是「跑分輸出」而是**錄下來的外部世界**：重錄要連網、燒 Tavily 額度，而且**錄不回同一份**
# （網路內容每天在變）。fixture 綁定一組環境（as-of 日期 + collection），是那批斷言唯一的依據；
# 掉了就等於那些斷言全部失效，且無法重現。llm replay cache 同理——沒有它，重放會生出
# 不同的 web query、打不到 fixture（MoE 不固定路由）。
!web_fixture.json
!web_fixture_news37.json
!web_replay_llm_news37.json

# 跑分 log（stdout dump）
*_log.txt

# Python 快取
__pycache__/

# ── 2026-09-01 補：**第五次**。上一段的警告寫著「新增 eval 輸入檔的同一次改動就要加白名單」 ─
#   · news_routing_questions.json  check_news_routing.py 的題目分類（flow / mixed / record）。
#     每一格 kind 是人工判的，而判錯的代價不對稱：把 flow 誤標成 record 會讓「拿財報冒充新聞」
#     被記成正確行為（col-08 就差點放錯，它的 KB 側有快照市值＝該並陳而不是該擋 web）。
#     腳本重現不了這個判斷。
!news_routing_questions.json

# ── 2026-09-01 補（同一次改動）：news 路由 before 臂的 fixture ─────────────────────
# 理由與上面〈web fixture〉那段一字不差：錄下來的外部世界，重錄要連網燒額度且錄不回同一份。
# 這一份綁 (collection=us_stock_rag_edgar_multiyear, as-of=2026-09-01)，
# 與綁 mdna／2026-08-20 的 web_fixture_news37.json **不可混用**（腳本的環境綁定檢查會擋）。
!web_fixture_newsroute_prod.json
!web_replay_llm_newsroute_prod.json
# after 臂的 llm replay cache。⚠ **必須與 before 臂分開**：共用的話 after 會重放 before 錄的
# plan 輸出（那是加 route 之前的 `list[str]`）→ 全部落到 kb ＝ 整個實驗自我作廢。
# web fixture 則刻意**共用**：同一份外部世界快照，兩臂才可比，且省 Tavily 額度。
!web_replay_llm_newsroute_after.json
# after2 臂（含「預算用完不退回 KB」與「replan 帶 route」兩個修正）的 llm replay cache。
# ⚠ **刻意用全新的 cache 而不是沿用 after 那份**：沿用會讓 plan 直接命中、路由決定被凍結，
#   於是量不到「router 判得穩不穩」——而那正是這一輪要驗的東西（mix-01 是 canary）。
!web_replay_llm_newsroute_after2.json

# ── 2026-09-03 補：**第六次**。上面兩段的警告都寫著同一句話，而我又漏了一次 ──────────
#   通則已經寫過三遍還是沒生效，所以這次改寫成可執行的：**錄完 fixture 的那一次 commit，
#   跑 `git check-ignore eval/<新檔>` 確認它沒被吃掉**——白名單是 opt-in，新檔預設會消失。
# mixed 題型的 prod fixture（21 題，collection=us_stock_rag_edgar_multiyear、as-of=2026-09-02）。
# ⚠ 綁 as-of 09-02，**不可與綁 09-01 的 newsroute_prod 混用**：`_url_published_date` 對未來日期
#   一律丟棄（d <= as_of），錯配會讓今天抓的頁面失去日期＝把人工痕跡烘進基線。
!web_fixture_mixed_prod.json
!web_replay_llm_mixed_prod.json
# probe_web_query_form 的 2x2 query 形式探針 fixture（r2 是第二輪，刻意另一份才比得出差異）。
!web_fixture_query_form.json
!web_fixture_query_form_r2.json

# ── 2026-09-04 補：**第七次**。上一段才把通則改寫成「錄完就跑 git check-ignore」──────
#   · chunk_gold.json  **chunk 層 gold**（41 題／83 顆）。判準與 reference_answers.json
#     一字不差：**腳本重現不了**。候選雖然是程式抽的，但「哪些 literal 算數」是逐題人工
#     驗證的（`verified` 欄位記著怎麼驗的），而剔掉的三種形狀——脈絡值、同表鄰居、純巧合
#     ——沒有任何規則分得出來（那是感知）。掉了就要重做一次 41 題的人工審核。
#   ⚠ 這次是靠 `git status` 沒看到檔案才發現的，**不是**靠上一段規定的 check-ignore
#     ——那條規則寫了但沒人執行得到。真正擋住它的仍然是「commit 前看一眼 status」。
!chunk_gold.json

# ── 2026-09-12 補：**第八次，而且這次的失敗方式最糟** ──────────────────────────────
#   · eval_harness_baseline.json  閘門 H3 凍結的生產常數值 ＋ 17 個抄寫點的複核理由。
#     判準同上：腳本重現不了（那 17 條 `reason` 是逐行看過才寫下的）。
#   ⚠ **前七次掉檔是「東西不見」，這次是「量尺安靜地變成恆真」**：`main()` 寫著
#     `... if BASELINE.exists() else {}`，所以在別人的 clone 上 H3 會**印綠的、零常數、零抄寫點**
#     ——那正是這支閘門自己存在的理由（量尺壞掉不可以與系統正常外觀相同）。
#     同一次改動已把 `main()` 改成缺檔就 FAIL（`--update-baseline` 除外）。
#   ⚠ 這次是在盤點「搬到另一台機器要帶什麼」時撞到的，**七段警告一段都沒攔住它**。
!eval_harness_baseline.json
