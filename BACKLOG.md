# BACKLOG — 未做 / 待決 / 已接受的極限

> **這裡放什麼**：還沒做的事、做了但沒定案的事、查清楚後決定**不修**的極限。
> **這裡不放什麼**：已完成的改動（→ [`CHANGELOG.md`](CHANGELOG.md)）、知識與踩坑（→ `docs/`）。
>
> **維護紀律**：一件事做完就從這裡**刪掉**並寫進 `CHANGELOG.md`；查清楚決定不做就搬到「已接受的極限」並寫下**復活條件**。
> 每一條都要能回答「**卡在哪**」——沒有卡點的條目不是 backlog，是願望。

---

## 量尺缺口（做別的事之前先看這裡：這件事現在量得到嗎）

- **「修了檢索有沒有用」沒有量尺，而這是現在最大的空缺。** 檢索層缺陷已經量得很清楚（`gold@5 0.878`、12.2% 可量題有缺陷、排序:召回 ＝ 4:1、漏斗的圈選層:檢索層 ＝ 5:2），但**「把它修好答案會不會變好」仍然沒有辦法量**。已知的反向證據：E1 逐題查過，被 RRF sweep 救回的三題**在 agentic 上本來就沒有損失**。
  **卡點**：agentic 的 `sources` 是跨子問題聯集不是 top-k 切片，而單管線那條路（使用者實際在用的）**答案層沒有任何 eval 涵蓋**。

- **`NOISE` 常數仍然只有一對樣本。** 三對裡有兩對推翻了前一對的某一格（`answer_correctness` .007 → .012、`nv` .008 → .012、`faithfulness` .010 → .021）。`context_precision` 那格最不可信（三對量到 .046 / .005 / .006，現在取最大的 .046）。
  **卡點是成本**：一輪全量在 `.venv-ragas` 要 ~2 小時（`--max-workers 2`，NVIDIA 限速不能再高）。**引用任何 `NOISE` 常數之前，先看它是幾對量出來的。**

- **`forced_pass` 要不要接成揭露／拒答，目前不能做。** 四輪分母落在 7.4~9.2%，但 R3／R4 各獨立量到**其中 ~2/8 是 Grader 判錯、事實就在 KB 裡**（`lex-04` 的 CUDA 2006、`mh-01` 的 GOOGL TTM 營收、`mh-04` 的 GOOGL 營業利益率——後者那顆 chunk 就在該子問題自己的候選池裡、答案還引用了它）。**接成拒答會把逐字有據的正確答案變成拒答。**
  **復活條件**：有一支能**分辨「Grader 判錯」與「KB 真的沒有」**的量尺（關鍵是陰性對照要能取樣到「生產判不夠但事實在 KB 裡」的子問題，可拿這 3 題當種子）。

- **「億」的發生頻率有分母了，但仍缺第二個對照臂。** 四輪 `unit_stats` ③ 是 10／23／23／28（7→8→15→12 題）＝ LLM 違反 Rule 11 的頻率**高且不穩**；① 四輪是 0／0／4／2。這個分母能回答「backstop 該不該存在」（答：該），**不能回答「哪一代模型比較糟」**——`gpt-oss-120b` 已 410，產不出對照臂。
  ⚠ **`None` 與 `{}` 不可合併**：`None` ＝ 那題沒經過後處理（崩潰降級），`{}`／全 0 ＝ 經過但沒觸發。合併會把降級題算進分母、讓頻率被系統性**低估**。

---

## 新開：Grader 圈選與 `COMMIT_TOP_K`（2026-09-11 的漏斗指出來的）

三段漏斗（`experiments/gold_funnel_r4_20260911.json`）：gold 落在「撈到了但被圈選／`COMMIT_TOP_K` 丟掉」**5 題**，落在「檢索根本沒撈到」**2 題**。同方向的證據還有兩個：`forced_pass` 的 Grader 誤判率 ~2/8；`sem-03` 在 RRF=30 下仍卡在圈選。
**卡點還是同一個**：「修了答案會不會變好」沒有量尺。**在有那把尺之前不要動 `relevant_ids` 的語意或放寬 `COMMIT_TOP_K`**——`docs/EVAL.md` 已試無效總表裡「單純放寬 Grader 保留率」是量過並否決的（+32% 證據字元 → correctness 動 0.001、precision −0.053）。

---

## 待決的決策

- **rubric 這條線要正式退役，還是把 rubric 補回 `eval_set.json`？** 實測 65 題**一題都沒有 `rubric`**（從 100 題定版那次就沒有），結果檔的 `correctness` 全為 null。⚠ 這不表示該刪——生成品質改由 RAGAS `answer_correctness` 負責是刻意的。**在決定之前不要再往 judge prompt 加規則**（沒有活的消費端，加了也量不到）。

- **judge 判不了「捏造」，因為它看不到來源。** `_CORRECTNESS_FEEDBACK_SYSTEM` 的規則寫著「not grounded in the sources」，而 `evaluate_correctness_with_feedback` **沒有任何參數會拿到 chunk** → 只能靠合理性判：九次觀測下離譜的（市佔率 92%）7/9 抓到、讀起來合理的（45% CAGR）**1/9**。
  ⚠ **不建議把 chunk 塞回 judge**（groundedness 已由 RAGAS faithfulness 負責，那正是當初移除的理由）。真要修該修**題目的 checkpoint 措辭**。與上一條綁在一起決定。

- **`eval/eval_agentic.py` 是死碼，要刪還是標退役。** 它 import 的 module 在 repo 裡已不存在。⚠ **刻意沒跟著修**：半修反而讓它看起來像活的。

- **`eval/replay_cache.json`／`web_fixture*.json`（359 KB）要不要納版控。** **傾向要**：它跟 `reference_answers.json` 是同一種東西（不需要唯一正確，只需要固定且對所有組態一視同仁），而且沒有它 `check_web_claims.py` 一條斷言都跑不了。卡點只有檔案大小。

- **複雜度 router**（簡單題→單發、複雜→agentic）。保守偏 agentic（誤判複雜為簡單代價高）。**卡點：measure-gated，目前量尺分不出。**

- **生產 `rag_query.py` 是否跟進 `full_translate_en=True`**（agentic 已固定開）。卡點：要先確認同樣的 rerank 平坦問題在單發管線也存在。

- **一致性 validator 的重寫路徑**：偵測 4/4 精準，但重寫實測 **2 好 1 壞**（`col-11` 修掉矛盾卻把總營收誤標成雲端營收，且三層驗證全過）。選項：改成**只偵測不重寫**（把矛盾標記給使用者看）。

- **Grader 的責任剝離（評判／重寫／圈選／時效分類拆成不同呼叫）要不要做。** 現況是**一次 LLM 呼叫**吐 `{sufficient, missing, new_query, relevant_ids}`（live 再加 `realtime_need`），下游四個消費端全部吃它。**2026-08-28 逐項查過，結論是現在不做**——主張要拆的三個理由各自被自己的量測削弱，剩下的只有可觀測性（而兩次診斷都靠 `AGENTIC_TRACE=true` 在同一個 session 內分辨出來）。
  ⚠ **反對的證據要一起記**：把 query 改寫權交給**有 agency 的節點**實測更差（乾淨 decomposition 35/75 vs ReAct 16/75）；把改寫路由給 Replanner 會餵養已記載的子問題爆炸級聯（實測 7 次 web／近一小時一題）。
  **復活條件（任一）**：① `realtime_need`／`sufficient` 在另一個沒測過的措辭族上再失手一次（那才是「責任壓太多在同一次呼叫上」的證據）② `probe_relevant_ids` 量到「被濾掉的是答案所在的 chunk」③ 子問題爆炸級聯在生產再現。
  **真要做時的形狀**：拆成**獨立的單一職責 rewriter 呼叫**，**不是**把改寫還給 Planner／Replanner。

- **`find_claim_conflicts` R2 放寬**（`len(seg_changes) >= 2` → ≥1）。卡點：要先量誤報率——「總計 < 某部門」在另一部門衰退時是**合法的**。

---

## 已知缺陷（查清楚了、還沒修）

- **`*_IncomeStatement_*.txt` 的財年標記只在 `#0`，`#1..#N` 完全沒有**（目前**無害但有潛在答錯路徑**）。那些檔是扁平的 key-value dump，切成 ~385 字元一塊，而 `--- Fiscal Year: YYYY ---` 只出現在第一塊。實例：`NVDA_IncomeStatement#1` 有 `Diluted EPS: $4.90`（FY2026）**整塊沒有年份**；`META_IncomeStatement#1` 是 `$23.49`（FY2025）、`#6` 是 `$23.86`（FY2024），**兩塊都沒有年份** ⇒ 撈到哪一塊就答哪一年；`#4` 的標頭出現在塊**中間** ⇒ 單一 chunk 橫跨兩個財年。
  **為什麼目前無害**：`lex-03`／`lex-07`／`lex-14` 三題都走 10-K 的損益表答對了。
  **修法方向**：ingest 時把財年標頭接地到**每一塊**，並把財年當硬邊界（＝filing 那條路的「期間章節」層在做的事）。
  ⚠ **要重跑 ingest（三小時＋三道閘門），所以等下次有正當理由重建時一起做。** ⚠ **在那之前不要為它加檢索層的補丁**（那會變成為一種檔案型別寫特例）。

- **`MSFT_10K_2024.html#158` 幅度接地在 chunk 層漏一筆**（1/1009＝0.1%，帶著上線）。把兩塊存檔文字餵回判斷式重放，**兩個合併條件都是 True** → 量尺沒錯，是真的「併得起來卻沒併」。
  **卡點＝根因要重跑切塊器才定得了案**：`_merge_small_chunks` 是逐硬邊界呼叫的，而閘門只看 `chunk_index - 1`（會跨邊界）→ 兩者對「相鄰」的定義不同。**復活條件**：下次有正當理由重跑 ingest 時（不要為了 0.1% 專門跑一次三小時重建）。

- **`_fill_dependent_hop` 把所有 `#N` 佔位符換成同一個實體。** `_DEP_PLACEHOLDER_RE.sub(entity, task)` 不看 N 是幾 → `在 #0、#1、#2 中，哪一家…` 變成 `在 NVIDIA、NVIDIA、NVIDIA 中，哪一家…`（零 LLM 可重現）。
  ⚠ **根因有兩層，只修上面那層沒有用**：`depends_on` 是單一 int，**結構上表達不了「這一跳依賴三個父 todo」**（閘門⑮ 的無環保證就是靠這個單值形式），而比較型的 hop 天生是多父的。
  ⚠ **`mh-03` 最後答對了**，所以任何端到端跑分都看不到——三家的 Fundamentals chunk 都在池子裡，Generator 自己讀著數字比對出來。只有 Planner 用 `#N` 時才發作（`mh-01`／`mh-02`／`mh-05` 是逐字寫出公司名的）。
  **修之前要先決定 `depends_on` 要不要改成 list。**

- **`_extract_citations` 對 `;` 串接的引用整段抓不到。** `_CITE_RE` 要求 chunk 編號後**緊接右括號**，所以 `[A.html, chunk #20; B.html, chunk #19]` 抓到的是 **0 筆**（不是部分）。65 題實測 363 個引用區段裡 5 個（1.4%）是這形狀。
  ⚠ **影響的是誰**：`citations` 是閘門㉑ 接受守衛的四項指紋之一，**這種形狀它一筆都看不到** ⇒ 重生成若在這裡捏造假來源，守衛不會發現（方向是**漏報**）。
  ⚠ **未修**：改 `_CITE_RE` 會動到㉑ 的指紋與既有結果檔的判定，要配斷言＋跑全部閘門。`probe_gold_funnel` 目前用前置正規化繞過並把差額單獨印出來。

- **`degraded_reason` 的 `_loc` 取的是無條件最深一層**，而 SDK 拋的例外最深一層住在 `openai/_base_client.py` ⇒ **它仍然沒說是哪個節點炸的**，而那正是閘門㉒d 宣稱要解決的事（R4 那次真正帶資訊的是**例外型別**不是檔:行）。
  **修法**：改成回報「**最深的 repo 內幀**」並保留絕對最深那一幀（兩個都印）。⚠ 動它要同時改 ㉒d 的斷言，並補一條誤報對照：**整條 traceback 都在 repo 外**時不可以回 `"?"`（那會比現在更糟）。

- **live 路徑仍會出貨少數指向不存在來源的引用。** 確定性還原 `Reference N → allowed_chunks[N-1]` 已修掉大部分，但**兩個守門條件擋下的那些刻意不動**（序號越界／`chunk #M` 與第 N 筆不一致）：走原本的重寫路徑，重試用完仍會原樣出貨。**這是已知且刻意的極限**——猜錯比不修更糟。

- **`web_ignored`：web 打了、回應可用，但答案一個 web 都沒引用。** 兩個獨立實例（`web-01` 第 10 輪拿 82 天前的快照當「目前」、`mh-09` 打三次 web 卻只有財報引用）。**成因在 Generator 的抽樣，不在輸入**（同一份 fixture 的其他輪都引用了它）。
  **卡點**：確定性守衛不好寫——「web 有內容卻沒被引用」在 web 真的回垃圾時是**正確行為**，兩者在結構層看起來一樣，要分辨得看內容層有沒有被問的那個量＝感知。
  相關：`_unfulfilled_web_route_gaps` 只做結構性判斷（`route ∈ (web, both)` 且 `web_notes` 為空），所以「web 回了一堆無關但非空的頁面」**不留缺口**，外觀與「web 成功回答了」完全相同。**這是設計範圍不是 bug**，記在這裡是為了讓這個洞有名字。

- **R4「兩邊都標了出處就閉嘴」那個分支實務上幾乎不會觸發**（方向安全）。兩個獨立理由讓它幾乎恆為 False：① `quote` 是「30 字內的原句片段」，而一則 web 引用光網址就遠超過 30 字元 ② `_WEB_MARK = "[web:"` 是**半形**，而實測 40 份答案的 web 引用**全部是全形**【web: …】。
  **方向是安全的**（誤報時要求的動作「把兩個值連同時點講清楚」本來就是正確行為，代價只是多燒一輪 reflect）。**為什麼沒當場修**：改判斷式要先有能證偽它的確定性測試，而 `quote` 是 LLM 產物、目前沒有 fixture 錄著真實的 claims 陣列。
  ⚠ 修法**不是**把 `_WEB_MARK` 加上全形就好——①才是主因。真正的修法可能是改判斷來源（看整份答案而不是 quote）。

- **多公司題沒有期別保護。** `_resolve_period_filter_llm` 遇到兩家以上直接回 None，因為 `build_qdrant_filter` 吃 flat AND list、**結構上寫不出 per-ticker 的 OR-of-ANDs**（`MatchAny` 是全域 OR，會讓 A 公司的舊季通過 B 公司的期碼）。**卡點是表達力。**
  **損害範圍只在單發管線**（agentic 的 Planner 把 8 題多公司題拆成 17 個單公司子問題），而 `api_server.py` 直接呼叫 `rq.retrieve()`——**使用者實際在用的 web UI 就是有損害的那條路**。
  ⚠ 真病灶是**席位競爭**不是期別（kind A ＝ 0、kind B ＝ 6）。`_ensure_ticker_coverage` 有保底但它補**該家分數最高的 chunk**（實測補進來的是 `AAPL_News`、`TSLA_Fundamentals`）。
  **收斂後的修法**：補位時若本次檢索帶 `routed_latest` 意圖，優先挑該公司**最新期碼的 10-Q**。⚠ `eval_set` 量不到（0 題）——這是產品修正不是分數修正。

- **`realtime_need="none"` 會短路整段時效日期算術。** `REALTIME_STALE_DAYS["none"] is None` → `_stale_for_realtime` 第一行就 return ⇒ 「KB 補不了就去 web」被一個三分類的 LLM 欄位單點守住。2026-08-28 改 prompt 後「近期動態」那個措辭族 0/3 → 5/5、陰性對照不動，**但這條沒有因此關閉**：修的是分類器在一個措辭族上的準確度，**不是那個單點依賴**。
  **為什麼沒修結構：目前測不出來。** KB 裡沒有任何一家的 filing 天花板超出申報週期，這條路徑在現有資料上永遠不會觸發。
  **復活條件**：① 有一題的 KB 天花板真的落後於申報週期 ② 先寫出能證偽它的真值表（`period_ref` × 天花板 × as_of）加進閘門⑤。
  ⚠ 不要用詞表判「這題是不是相對期間指稱」。可行方向是 Grader 加一個與 `realtime_need` **正交**的 `period_ref` 欄位，但**只能加在 live block**（動到 snapshot 的 Checker prompt 會破壞 eval 基準逐字不變）。

- **KB 真的沒有資料時，replanner 仍會生一串同義待辦。** `kb_unfixable` 只涵蓋**時效**這一種「KB 補不了」。**成本已經不同了**（web 被 `QUERY_WEB_BUDGET` 封在 3 次，多出來的只有本機檢索），所以優先度低。⚠ **不要用字串相似度認同義待辦**（分離度是負的）。可行方向是把 `kb_unfixable` 推廣到「同一子問題連續兩輪 `missing` 沒有實質變化」——但那是啟發式，要先有能證偽它的測試。

- **web 結果的外國掛牌「路徑」擋不到。** `_host_allowed` 擋掉了地區**子網域**，但同一主機下的外國掛牌**路徑**擋不到（實測 `stockanalysis.com/quote/bvl/AAPL/market-cap` 利馬交易所 $4.98T vs 美股頁 $4.45T，**同一天差 12%**）。
  ⚠ **不要用路徑詞表擋**（各站自訂的 URL 慣例，改版就失效）。**對照：同類但已解的是選擇權合約頁**——那有 OCC 標準格式可比對（格式定義的封閉集合），外國掛牌路徑**沒有**跨站標準。
  **現況風險有限**：`TAVILY_PER_DOMAIN_CAP=2` 讓同站最多兩則，但沒有保證。

- **重述（restatement）無法偵測。** 10-K 會因改分部結構／會計政策**重述前一年數字**（實測 `MSFT_10K_2026` 的補充未審計表帶著 `219,790`／`87,464`，原始值是 `211,915`／`105,362`）。**值不存在 ≠ 事實不存在。** 這與「`filing_date` 沒進 payload ＝ 沒有 transaction time」是同一個根（雙時間軸），目前沒有任何一般性機制。

- **「證據尾巴」在 consumer 側仍有四份定義**（`check_number_defects.FOOTER`、`check_rounding_fidelity._TAIL_RE`、`eval_ragas_vs_rubric` 的 footer strip、`run_agentic_on_evalset` 那份）。正式定義已收攏成 `rq.strip_evidence_tail()`，**新消費端一律用它**；**producer 側已經乾淨**。**卡點**：動 RAGAS 那份會移動分數，要單獨做並重新取基準。

- **Planner 對「KB 結構上不可能有」的題仍白搜一輪。** ⚠ **不要讓 Planner 自己判**（它手上沒有真實 chunk，猜錯的代價不對稱）。**剩餘收益已經很小**：`kb_unfixable` 已把浪費從 21 輪壓到 4 輪，而那 4 輪是每個子問題各 1 次首輪檢索＝必要的。

- **`mix-07`（Plan 把混合題譯成純新聞查詢）。** 真因是 Plan 把「Wiz 收購金額」這個**財報事實**譯成新聞查詢 → 檢索被限制在 News chunk（六個封存 run 裡「只撈到 News」與拒答 6/6 完全相關）。⚠ 掛了 replay fixture 也擋不住它（replan 產生的新 query 不在快取裡）⇒ **不能當 collection A/B 的判準**。KB 拔除新聞後已 2/2 翻成 PASS，但成因（plan 抽樣變異）沒有消失。**卡點**：planner prompt 類改動的效果在現有量尺下量不出來。

- **`lex-17`（ratio 題的殘留）。** 檢索半邊已修（確定性補撈 Fundamentals `#0`），剩下的是**開頭第一句仍以 10-K 財年 18% 當結論**，而 gold 要的是 TTM 18.30%。⚠ `anchored_pct` 現在是靠 `expect_text` 析取才 PASS——**它和 `require_text` 幾乎重複，名字說的事情已經不再量了**（量尺放寬的常見代價）。**卡點與 `mix-07` 同型**，判讀**必須跑 ≥2 輪看 FAIL 比例**。

---

## 觀察（記著，但刻意沒有登錄成斷言）

- **兩題「打了 web 卻沒引用」尚未判定**（`mh-07` 誠實說「新聞稿中並未出現」、`mh-09` 自己標明「現有文件未直接披露」）。**沒有登錄**：web 有沒有撈到可用內容**不在系統控制內**，斷言它等於把外部世界寫進量尺。

- **Fundamentals 在口語問法下擠不進 top-k。** 同一支探針測四種問法問 MSFT 營收成長率：「…表現如何？」`#0` 在 rank 5、「最近一年的營收年增率」rank 1，但口語的「微軟的營收成長率是多少？」**完全沒進 top-5**。**沒有登錄**：10-Q 的季度成長是合法的另一種口徑。**卡點是這個判斷本身**——要先決定「使用者沒指定口徑時該給 TTM 還是財期」，那是 eval 設計問題不是檢索問題。**決定之前不要動檢索。**

- **補進 Fundamentals `#0` 之後那 6 題的 correctness 平均 −0.029，需要逐題人工複審**（`mix-01`／`mix-06`／`mix-08`／`mix-10`／`lex-17`／`col-12`）。⚠ **不要拿這個 −0.029 當結論**：n=6 而單題波動輕易到 ±0.3。要看的是「gold 的口徑是財期還是 TTM」與「答案用了哪一個」。

---

## 環境雜務

- 刪除過期的 Qdrant collection（**保留 `us_stock_rag_edgar_exp4` 當歷史基準**）。
- `git prune`：目前有過多 unreachable loose objects（每次 commit 都會警告）。

---

## 已被證據關掉的決策（量完了，不要再提）

| 曾經想做 | 量到什麼 | 復活條件 |
|---|---|---|
| **把 `MAX_TODOS` 拆成 Planner／Replanner 兩份額度** | 四輪 300+ 個 replan round 一共只擋掉 **2 個**待辦（全在 R1），而那 2 個撈的是**已經在 collected 裡**的 chunk | `refused_budget` 升到兩位數，或出現一筆被擋掉、而該資訊確實不在 collected 裡的待辦 |
| **把 Synthesize 改成 critique↔refine 迴圈（DeCRIM）** | R3 首次出現退回（24 次重生成 1 次，`lex-03` 的 `untraceable 0→2`）⇒ 鏈確實會互相破壞，**但閘門㉑ 的接受守衛零成本地擋下了它**。迴圈要買的是「退回之後剩下的缺陷修不修得掉」，而那**仍然沒有量尺**，且每輪要多燒一次 LLM | 有了「退回之後的殘留缺陷」的量尺 |
| **拿掉「Planner 說 null 但句子沒主詞仍延後」那個例外** | `deps_disagreed` 四輪 2／1／2／1 ＝ 確實會發生；`deps_pruned` R4 首次非 0（Planner 吐了 `id=4 depends_on=4` 自我依賴，位置規則當場剪掉）⇒ 沒有合法依賴被誤剪 | 分歧數升高，或那個例外被證明把對的題弄壞 |
| **`RRF_TOP_N_PRIMARY` 再往上調** | 50 相對 30 是**純粹的浪費**（逐題 k/n 逐字相同、+135% 時間）；`FETCH_N` sweep 對 `retrieve()` 的回傳**零效果**（串聯卡在 RRF 名額） | 有證據顯示 30 名之後仍有 gold |

---

## 明確**不要**跑的

| 不跑 | 理由 |
|---|---|
| RAGAS 全量 | `.venv-ragas` 一輪 ~2 小時、六指標五個飽和、MDE 換算 4~7 題 |
| HyDE | 召回失敗只剩 1 題（`sem-05`），而排序才是主體（4:1）；`col-07`（唯一有 HyDE oracle 陽性的題）**沒有 chunk gold、量不到** |
| `eval_set_news.json` 跑分 | eval 預設 web 關著，跑了必然全滅，那不是證據 |
| `ablate_retrieval_model.py` | `MODEL_BIG` 已 410，刻意不修（換活模型＝把舊結論掛到沒跑過的組態上） |
| `gen_reference_answers.py --force` | 不帶 `--ids` ＝ 洗掉 24 處人工校正（現在會 exit 2，但別去繞過它） |
| 為 `kb_unfixable`／`realtime_need` 在 snapshot 下加逐輪觀測 | 那兩個欄位算在 `if _live:` 裡面，snapshot 下只會錄到三個常數＝零資訊（閘門⑰） |

---

## 已接受的極限（要動請先滿足復活條件）

### 不做「內容天花板旗標」（2026-09-11 查清後決定）
**原本的計畫**：`forced_pass` 的子問題有 8/10 在 3 倍寬的檢索下 Grader 仍判不足 ⇒ 做一個「KB 內容裡真的沒有」的旗標，而探針量到陰性對照的內容誤殺是 **0/15**。
**推翻它的證據**：逐筆讀 `forced_pass_detail` 並**直接讀 Qdrant 驗證**（不採信答案的引用宣稱）——`lex-04` 的「CUDA 推出年份」逐字寫在 `NVDA_10K_2026.html#20`；`mh-01` 的「Alphabet TTM revenue」寫在 `GOOGL_Fundamentals#0`。⇒ **誤殺率至少 2/8 ＝ 25%**，而那個旗標若在 `lex-04` 上亮，會把一個**逐字有據的正確答案變成拒答** ⇒ **比現況更糟，證據方向是反的**。
⚠ **那個 0/15 為什麼抓不到**：陰性對照抽樣自**生產判夠**的子問題，而危險的形狀正好是「生產判**不**夠、但事實在 KB 裡」——**取樣框架結構上排除了它**。
**看起來是真的內容天花板的**（仍無旗標，但不急）：`sem-14`（智慧型手機／平板市佔比例）、`mix-12`（中國資料中心晶片**出貨量**）、`col-06`（競爭車廠的自駕功能名稱）——共同形狀是「10-K 依規定不會揭露的東西」。
**復活條件**：見上方〈量尺缺口〉那條。

### 集合詞題（「Magnificent Seven」「這些科技巨頭」）不支援
**使用者決定縮限題型範圍**（2026-09-08）。**確定性的成因鏈**：`_mentioned_tickers` 回 `[]` → `if len(mentioned) > 1` 不成立 → **每家保底完全不觸發** → top-5 席位隨機裝、連跑三輪 `sufficient=False` → Generator 拿不完整的資料比大小 → 答 Apple（$122.58B），正解是 GOOGL（$160.21B）。
**對照組**：「在 NVIDIA、Meta、Alphabet 這三家中…」→ 保底觸發 → 答對。**界線就是這一句：公司名寫在題面上就對得了。**
⚠ `check_comparison_claims` 的全 PASS **不能外推到這一類**（那 5 題 multi_hop 全是「列在題面上」那一型）。⚠ **順帶撞到 `MAX_TODOS`**：七個成員逐一列滿要 7 個 todo，加上第二跳就是 8 > 7，**結構上列不滿**。
**復活條件**：① 集合詞題進了題庫或產品端真的有人這樣問 **或** ② `forced_pass` 決定要做揭露／拒答——那時這一類會**自動變成誠實的拒答**，對**所有**「候選集合要系統自己決定」的題都有效。
⚠ 真要解的話，正解**不是**硬編一張「七巨頭＝這七家」的詞表（成員會變），是讓 Planner 把成員當結構吐出來。

### 不改寫 commit `16d4646`（實驗檔進版控那次）
`16d4646` 加了 161 個檔／225,302 行，隨後 `dad5544` 又刪掉——一次「加進來又刪掉」留在歷史裡。**決定不做**，三個理由：① 它**已經 push 出去了**（改寫要對共用遠端 force-push，會弄壞每一份既有 clone）② 壓縮只有在所有人重新 clone 之後才拿到好處 ③ **代價根本不痛**（`.git` 現在 55 MB）。
**復活條件**：① 這條分支確定不會被別人 clone ② `.git` 真的大到造成困擾（~500 MB）。屆時在**另一份 clone** 上跑 `git filter-repo`——**不要在工作目錄上直接改寫歷史**。⚠ 這件事**由使用者決定、不由我代勞**（force-push 是對外、難以回復的動作）。

### 其他

| 項目 | 真因 | 復活條件 |
|---|---|---|
| **切塊／ingest 不再改** | **量尺飽和**：六個 RAGAS 指標五個已達或超過 gold 上限；檢索全修到完美只值 +0.024，低於 0.067 噪音底線 | 換沒飽和的量尺（擴充 `check_number_defects` 覆蓋率），或改攻生成層 |
| **KB 不收新聞** | 新聞的正確性判準與本系統的每一層機制正交；實測它佔 2.5% 語料卻造成最大的殘留檢索失敗，且 28 條來源標記有 17 條不合本專案自己的 web 白名單標準 | **不是「把舊語料放回去」**。路由已驗（37 題觸發 web ≥35/37、陰性對照 0/7）、內容已驗（5 個 FAIL 全是本地的引用缺陷）→ **沒有任何一項證據支持把新聞加回 ingest** |
| **排序層加時間衰減** | 全域 decay 假設「越新越好」，而加入舊資料的目的正是要能回答歷史題。`relative` 那類已證明**期間解析得到時根本不需要動排序層**（兄弟密度漲 4 倍、指標一格未動）。業界那些 recency postprocessor 的場景是**沒有明確指定期間的新聞流** | 提得出「只在期間無法解析時生效」的版本 ＋ **歷史題陰性對照**證明沒把它們弄壞 |
| **跨期近重複去重／MMR** | 殘留的排擠是**定義出來的**不是缺陷（剩下 48 席全是 `keep=2` 刻意允許的第二席，沒有任何一組 >2 ＝ collapse 零漏抓），而那第二席不是浪費：`keep=1` 讓趨勢題期別覆蓋 −41% | 要有一把**不是 metadata 分組**的尺（內容層近重複） |
| **曆年 vs 財年的年份歧義** | `fiscal_year` filter 只認財年，於是「Microsoft 2025 年的季報」（曆年語意）會降級到 Tier 2。實測**內容沒變差**，變的是多一句揭露語 | 要真正解掉得讓 `parse_query_filters` 講出「這個年份是財年還是曆年」——那會動到 replay 基準與 65 題的檢索結果，**目前判斷不值得** |
| **期別探針的 gold 不再細修** | 人工複審：**46/49 的 gold 就是該公司最新那一份**，而 collapse 的規則正是「留最新」→ `gold@k` **結構上偏袒它**；3 題陰性對照經人工讀原文**全是假警報** | ⚠ 這個偏袒現在跑在生產 collection 上。復活條件是「下一次要用 `gold@k` 的差值做決定時」。要修就得重寫 gold 到 **chunk 粒度並標註「哪些期別同樣可接受」**＝重做題庫等級的工 |
| **`faithfulness` 不再追高** | 這是**負空間**：把 gold 當答案餵回去只拿 .726，輸給系統的 .876。往上推等於要求系統比標準答案更保守 | 換 metric 才談 |
| **`us_stock_rag_edgar_exp4` 的 Fundamentals 比率仍是小數** | exp4 ＝ 歷史基準，早就不是任何入口實際查的東西 | 若要拿 exp4 當對照臂，得先跑 `migrate_fundamentals_pct.py` |
| **`sem-08`**（AMZN AWS 策略方向） | 三種系統側修法皆敗於同一真因：生成模型判定「獲利數字」與「策略方向」問法無關而主動略過，**非訊號埋沒** | 只剩調整 rubric 或維持現狀 |
| **`col-07`**（否定框架） | 病根是「非龍頭」vs「minority share」的**框架落差**，不是翻譯；`full_translate_en` 已開仍漏。只有已知答案字面（＝HyDE）才撈得到 rank 1 | 不為單題上 HyDE |
| **`col-08`**／**`col-05`**（口語版） | dense rank 25/106，落差超出 rewrite 射程 | 有新召回機制才重試 |
| **`col-04`**（Azure 口語版） | 關鍵內容在 top-5，生成被口語框架帶偏、選擇誠實拒答。**非 bug** | 觀察中 |
